using UnityEngine;

public enum PlayerId
{
    One = 1,
    Two = 2,
    Three = 3,
    Four = 4
}

public enum GameType
{
    Collect,
    Sort,
    ChangeRoom
}

public enum SortColor
{
    Red,
    Blue
}